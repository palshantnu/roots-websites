import { ArrowRight } from 'lucide-react'
import { authors } from '../data'
export default function AuthorsPage({ Layout, PageIntro }) { return <Layout><main className="catalog-page"><PageIntro eyebrow="THE RTS AUTHOR COMMUNITY" title="Strong minds behind every page." copy="Meet the writers, researchers, poets and thinkers whose work has found a home with RTS Publication." /><div className="author-grid-new author-catalog">{authors.concat(authors).map((author, index) => <a href={`/authors/${author.slug}`} key={`${author.id}-${index}`}><div className="author-photo"><img src={`https://images.unsplash.com/photo-${author.image}?auto=format&fit=crop&w=500&q=85`} alt={author.name} loading="lazy" /></div><h3>{author.name}</h3><p>{author.role} · {author.books} books <ArrowRight size={13} /></p></a>)}</div></main></Layout> }
